export class Music {
  image:string
  name:string
  preview:string
  title:string

  constructor(image:string, name:string, preview:string, title:string) {
    this.image = image
    this.name = name
    this.preview = preview
    this.title = title
  }
}
